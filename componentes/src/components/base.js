export class xxxComponent {
    constructor (title) {
        this.title = title
    }
    renderString() {
        const xxx = 
        `<header>
            <h1 class="title">${this.title}</h1>
        </header>
        `
        return xxx
    }
}